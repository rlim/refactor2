scripts in the Loading directory will load data in the normal fashion and return a copy

The retrieval directy will create the user selected variable name in the calling functions 
workspace, and set the contents without returning a copy.


